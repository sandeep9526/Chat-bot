[pytest]
markers =
    slow: hits a real LLM via OpenRouter — slower, and can occasionally fail if every free-model fallback is briefly rate-limited (not a flaky test, a real characteristic of call_llm's fallback chain).
